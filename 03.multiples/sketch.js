function setup() {
  createCanvas(600, 400);
}

function draw() {
  background(300);
  
  textFont('Times New Roman')
  textAlign(CENTER);
  textSize(16)
  text('Please enter your username', 300, 150)
  
}